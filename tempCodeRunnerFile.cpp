   // void add(const string &date, const string &text) {
    //     string dt = parse_date(date);
    //     bool dt_exist = data.count(dt) > 0;

    //     if (dt_exist) {
    //         data.insert(pair<>(dt, text));
    //     }
    //     data.insert({dt, text});
    //     cout << "successfully added";
    // }